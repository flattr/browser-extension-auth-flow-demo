{
  "name": "browser-extension-auth-flow-demo",
  "private": true,
  "devDependencies": {
    "@rollup/plugin-replace": "^2.3.4",
    "rollup": "^2.33.1",
    "rollup-plugin-chrome-extension": "^3.4.0"
  },
  "scripts": {
    "build:dev": "rollup -c",
    "watch:dev": "rollup -c -w",
    "build:prod": "rollup -c --environment TARGET_ENV:production",
    "watch:prod": "rollup -c -w --environment TARGET_ENV:production"
  }
}
